% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null_distribution.R
\name{test_decay_significance}
\alias{test_decay_significance}
\title{Test whether a specific anchor position shows an unusual group effect}
\usage{
test_decay_significance(
  combined,
  anchor_position,
  genome_length,
  n_random = 500L,
  window = NULL,
  exclude_radius = 0,
  seed = NULL,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{combined}{A data.frame or tibble from \code{\link[=load_all_samples]{load_all_samples()}}.}

\item{anchor_position}{Non-negative numeric scalar. Anchor
coordinate of scientific interest, in base pairs (e.g. OriLyt
coordinate).}

\item{genome_length}{Positive integer. Reference genome length,
used to bound random anchor sampling.}

\item{n_random}{Positive integer. Number of random anchor positions
used to build the null. Default 500.}

\item{window}{Optional positive numeric scalar. Restrict the fit to
bins within \code{window} bp of the anchor. Passed through to both the
observed fit and every random-anchor fit so the two are
comparable.}

\item{exclude_radius}{Non-negative numeric scalar. If positive,
exclude a band of width \code{2 * exclude_radius} centered on
\code{anchor_position} from the random-anchor pool. Default 0 (no
exclusion). Set to \code{window} (or larger) to guarantee no overlap
between random-anchor windows and the observed-anchor window.}

\item{seed}{Optional integer. Random seed for reproducibility of
the sampled random anchor positions.}

\item{BPPARAM}{A \code{BiocParallel::BiocParallelParam} instance
controlling parallelism. Default \code{\link[BiocParallel:SerialParam-class]{BiocParallel::SerialParam()}}.}
}
\value{
An S3 object of class \code{"decay_significance"}, a named list
with elements:
\describe{
\item{\code{anchor_test}}{The \code{decay_test} object at
\code{anchor_position}, as returned by \code{\link[=test_decay_slopes]{test_decay_slopes()}}.}
\item{\code{null_F}}{Numeric vector of F-statistics from the random
anchor positions (successful fits only).}
\item{\code{empirical_p}}{Numeric scalar. Empirical p-value for
the observed F under the null distribution.}
\item{\code{anchor_position}}{Anchor coordinate echoed for
provenance.}
\item{\code{genome_length}, \code{n_random}, \code{window}}{Inputs echoed for
provenance.}
}
}
\description{
End-to-end wrapper for anchor-specificity testing. Fits the LMM at
the anchor position of interest to obtain an observed F-statistic
for the distance-by-group interaction, then builds a null
distribution of F-statistics at \code{n_random} random anchor positions
and returns an empirical p-value.
}
\details{
This is the primary tool for answering the scientific question:
"does coverage decay around this specific anchor differ between
treatment groups by more than would be expected at a random
genomic position?"

The empirical p-value complements, rather than replaces, the LMM
p-value at the anchor. The two answer different questions:
\itemize{
\item The LMM p-value asks: "given the observed data, how surprising
is the observed group-by-distance interaction under the null
that groups share a common slope?"
\item The empirical p-value asks: "how surprising is the observed
F-statistic under the null that there is nothing special about
this anchor position, i.e. we would see a similar effect at any
random genomic position?"
}

A small LMM p-value combined with a large empirical p-value
indicates a real group effect that is not specific to the anchor.
A small empirical p-value with a small LMM p-value is the
publication-grade result: the effect is real \emph{and} localized to
the anchor.

Runtime is dominated by the \code{n_random} LMM refits inside the null
distribution, so use \code{BPPARAM = BiocParallel::MulticoreParam()} for
meaningful \code{n_random}.
}
\examples{
\donttest{
## Illustration only; this call is expensive because it refits the
## LMM at n_random + 1 anchor positions. For real analyses, use
## n_random = 500 or higher and pass a parallel BPPARAM.
# combined <- load_all_samples(meta, base_dir, bin_size = 1000,
#                              genome_length = 238080)
# res <- test_decay_significance(
#     combined,
#     anchor_position = 93000,       # HCMV OriLyt (verify vs. your reference)
#     genome_length   = 238080,
#     n_random        = 500,
#     window          = 50000,
#     exclude_radius  = 50000,
#     seed            = 2026,
#     BPPARAM         = BiocParallel::MulticoreParam(workers = 8)
# )
# res$empirical_p
}

}
\references{
Phipson B, Smyth GK (2010). Permutation p-values should never be
zero: calculating exact p-values when permutations are randomly
drawn. \emph{Statistical Applications in Genetics and Molecular Biology}
9(1), Article 39. \doi{10.2202/1544-6115.1585}
}
\seealso{
\code{\link[=test_decay_slopes]{test_decay_slopes()}}, \code{\link[=null_distribution_F]{null_distribution_F()}},
\code{\link[=empirical_pvalue]{empirical_pvalue()}}.
}
