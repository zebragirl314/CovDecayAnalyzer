% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decay_plots.R
\name{plot_coverage_decay}
\alias{plot_coverage_decay}
\title{Plot normalized coverage vs distance from anchor, colored by group}
\usage{
plot_coverage_decay(
  combined,
  anchor_position = NULL,
  window = NULL,
  show_points = TRUE,
  loess_span = 0.3,
  palette = "Set1"
)
}
\arguments{
\item{combined}{A data.frame or tibble from \code{\link[=load_all_samples]{load_all_samples()}}.
Must contain columns \code{sample}, \code{group}, \code{bin_mid}, and \code{norm_cov}.
May optionally contain \code{distance_from_anchor}.}

\item{anchor_position}{Non-negative numeric scalar. Reference
coordinate for distance calculation. Ignored if \code{combined}
already contains \code{distance_from_anchor}.}

\item{window}{Optional positive numeric scalar. If supplied,
restricts the plot's x-axis (via \code{coord_cartesian}) to
\verb{[0, window]}. Data outside the window are still fit by the loess
smoother but are not shown; this is preferable to filtering the
data before smoothing, which would truncate the smoother's
support and introduce edge artifacts.}

\item{show_points}{Logical. If \code{TRUE} (default), overlay individual
per-bin points on top of the group trends.}

\item{loess_span}{Numeric in \verb{(0, 1]}. Passed to \code{geom_smooth(span = loess_span, method = "loess")}. Default 0.3.}

\item{palette}{Character scalar. Name of a
\code{RColorBrewer}-compatible palette used to color groups. Default
\code{"Set1"}.}
}
\value{
A \code{ggplot} object. Not printed automatically; call \code{print()}
or assign into a knitr code chunk, or save with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.
}
\description{
Produces a ggplot showing each sample's per-bin normalized coverage
as a function of distance from the anchor position, with a
loess-smoothed trend line per treatment group. This is the primary
visual diagnostic for coverage decay analyses.
}
\details{
The plot layers, in order:
\itemize{
\item Optional per-bin points (\code{geom_point}) with low alpha, so
individual observations are visible but the group trends dominate.
\item A loess-smoothed line per group (\code{geom_smooth}) with 95\% confidence
ribbon. Loess is used rather than the log-linear fit from the
formal test because visualization benefits from a nonparametric
smoother that can reveal deviations from linearity.
}

The loess \code{span} parameter controls the degree of smoothing: smaller
values follow the data more closely and can reveal local structure,
larger values produce smoother curves. The default of 0.3 works well
for a full-genome view at 1 kb binning; use 0.5 or higher for very
small windows where 0.3 would be unstable.

If \code{combined} already contains a \code{distance_from_anchor} column, it is
used as-is. Otherwise, \code{anchor_position} is required and distances
are computed via \code{\link[=add_distance_from_anchor]{add_distance_from_anchor()}}.
}
\examples{
## End-to-end example on the shipped simulated data. The WT+GCV
## samples decay six times faster than the WT-GCV samples, so the
## two loess curves should be clearly separated.
extdata_dir <- system.file("extdata", package = "CovDecayAnalyzer")
meta <- read.csv(
    file.path(extdata_dir, "sample_metadata.csv"),
    stringsAsFactors = FALSE
)
meta$coverage_file <- breseq_coverage_paths(meta$sample, extdata_dir)
combined <- load_all_samples(
    sample_metadata = meta,
    bin_size        = 100,
    genome_length   = 2000
)
plot_coverage_decay(combined, anchor_position = 1000, window = 1000)

}
\seealso{
\code{\link[=plot_decay_slopes]{plot_decay_slopes()}}, \code{\link[=plot_null_distribution]{plot_null_distribution()}}.
}
