% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_io.R
\name{load_all_samples}
\alias{load_all_samples}
\title{Load, bin, and normalize coverage for a set of samples}
\usage{
load_all_samples(
  sample_metadata,
  bin_size = 1000L,
  genome_length,
  epsilon = 1e-05,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{sample_metadata}{A data.frame or tibble with, at minimum,
three columns:
\describe{
\item{\code{sample}}{Character. Sample identifier (must be unique).}
\item{\code{group}}{Character. Treatment or condition label used in
downstream group comparisons.}
\item{\code{coverage_file}}{Character. Full path to the sample's
coverage file.}
}
Additional columns are preserved in the output and can be used
for downstream filtering or plotting.}

\item{bin_size}{Positive integer. Bin width in base pairs. Default
1000.}

\item{genome_length}{Positive integer. Reference genome length in
base pairs.}

\item{epsilon}{Positive numeric. Log-transform offset. Default
\code{1e-5}.}

\item{BPPARAM}{A \code{BiocParallel::BiocParallelParam} instance
controlling parallelism. Default \code{\link[BiocParallel:SerialParam]{BiocParallel::SerialParam()}}.}
}
\value{
A tibble with one row per (sample, bin), columns from
\code{\link[=normalize_coverage]{normalize_coverage()}} plus:
\describe{
\item{\code{sample}}{Character. Sample name.}
\item{\code{group}}{Character. Group label.}
\item{...}{Any additional columns from \code{sample_metadata} (except
\code{coverage_file}, which is dropped from the output).}
}
}
\description{
Given a sample metadata table with a \code{coverage_file} column giving
the path to each sample's coverage file, loads every sample's file,
bins and median-normalizes each sample independently, and returns
one combined tibble with sample and group metadata attached. This is
the standard entry point to the package.
}
\details{
The metadata table is the single source of truth for what to load
and where each file lives. This decouples the package from any
particular on-disk layout: files can live anywhere, be named
anything, and come from any tool that writes the required columns
(\code{position}, \code{unique_top_cov}, \code{unique_bot_cov}, \code{redundant_top_cov},
\code{redundant_bot_cov}).

Users with breseq output in the standard layout can build the
\code{coverage_file} column with \code{\link[=breseq_coverage_paths]{breseq_coverage_paths()}} in one line
(see examples). Users with any other layout can fill the column
directly with \code{file.path()} or \code{list.files()}.

Samples whose file is missing, unreadable, or does not contain the
required columns are skipped with a warning rather than aborting
the whole load.

Parallel processing is delegated to \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}}. The
default \code{\link[BiocParallel:SerialParam]{BiocParallel::SerialParam()}} processes samples
sequentially. To use multiple cores, pass a
\link[BiocParallel:MulticoreParam]{BiocParallel::MulticoreParam} or \link[BiocParallel:SnowParam]{BiocParallel::SnowParam} instance.
}
\examples{
## Example 1: breseq output in standard layout, using the shipped
## example data.
extdata_dir <- system.file("extdata", package = "CovDecayAnalyzer")
meta <- read.csv(
    file.path(extdata_dir, "sample_metadata.csv"),
    stringsAsFactors = FALSE
)
meta$coverage_file <- breseq_coverage_paths(
    sample_names = meta$sample,
    base_dir     = extdata_dir
)
combined <- load_all_samples(
    sample_metadata = meta,
    bin_size        = 100,
    genome_length   = 2000
)
head(combined)

## Example 2: arbitrary layout, paths filled in directly.
custom_meta <- data.frame(
    sample        = meta$sample,
    group         = meta$group,
    coverage_file = meta$coverage_file,
    stringsAsFactors = FALSE
)
combined2 <- load_all_samples(
    sample_metadata = custom_meta,
    bin_size        = 100,
    genome_length   = 2000
)
identical(combined, combined2)

}
\seealso{
\code{\link[=breseq_coverage_paths]{breseq_coverage_paths()}} for building \code{coverage_file} from a breseq
output tree;
\code{\link[=add_distance_from_anchor]{add_distance_from_anchor()}} and \code{\link[=fit_decay_slopes]{fit_decay_slopes()}} for the next
steps of the workflow.
}
