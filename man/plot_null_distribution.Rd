% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decay_plots.R
\name{plot_null_distribution}
\alias{plot_null_distribution}
\title{Plot the random-anchor null F distribution with the observed value marked}
\usage{
plot_null_distribution(sig, bins = 40L, observed_color = "#D7191C")
}
\arguments{
\item{sig}{A \code{decay_significance} object as returned by
\code{\link[=test_decay_significance]{test_decay_significance()}}.}

\item{bins}{Positive integer. Number of histogram bins. Default 40.}

\item{observed_color}{Character scalar. Color of the vertical line
marking the observed F-statistic. Default \code{"#D7191C"} (red).}
}
\value{
A \code{ggplot} object.
}
\description{
Given a \code{\link[=test_decay_significance]{test_decay_significance()}} result, draws a histogram of the
null distribution of F-statistics from random anchors and overlays a
vertical line at the observed F-statistic at the anchor of interest.
The empirical p-value is displayed in the plot title.
}
\details{
This is the most direct visual representation of the empirical
p-value method: the reader can see at a glance whether the observed
F falls in the bulk of the null distribution (unremarkable) or in
the tail (statistically significant for anchor specificity).
}
\examples{
set.seed(1)
fake_sig <- structure(
    list(
        anchor_test     = list(F_stat = 8.5, p_value = 0.002),
        null_F          = rf(500, df1 = 3, df2 = 40),
        empirical_p     = 0.006,
        anchor_position = 93000,
        genome_length   = 238080,
        n_random        = 500,
        window          = 50000
    ),
    class = "decay_significance"
)
plot_null_distribution(fake_sig)

}
\seealso{
\code{\link[=test_decay_significance]{test_decay_significance()}}, \code{\link[=null_distribution_F]{null_distribution_F()}}.
}
