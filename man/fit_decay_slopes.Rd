% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decay_slopes.R
\name{fit_decay_slopes}
\alias{fit_decay_slopes}
\title{Fit per-sample log-linear decay slopes (diagnostic)}
\usage{
fit_decay_slopes(
  combined,
  anchor_position = NULL,
  window = NULL,
  min_bins = 5L
)
}
\arguments{
\item{combined}{A data.frame or tibble from \code{\link[=load_all_samples]{load_all_samples()}}.
Must contain columns \code{sample}, \code{group}, \code{bin_mid}, and
\code{log_norm_cov}. If a \code{distance_from_anchor} column is already
present, \code{anchor_position} is ignored.}

\item{anchor_position}{Non-negative numeric scalar. Reference
coordinate for distance calculation. Ignored if \code{combined}
already has a \code{distance_from_anchor} column.}

\item{window}{Optional positive numeric scalar. If supplied, only
bins within \code{window} base pairs of the anchor (inclusive) are
used in the per-sample fit. Useful for restricting analysis to a
local region around the anchor (e.g. \code{window = 50000} for a
50 kb window around OriLyt). If \code{NULL}, all bins in \code{combined}
are used.}

\item{min_bins}{Positive integer. Minimum number of usable bins
required to attempt a fit for a given sample. Samples with fewer
than this are skipped with a warning. Default 5.}
}
\value{
A tibble with one row per successfully-fit sample:
\describe{
\item{\code{sample}}{Character. Sample name.}
\item{\code{group}}{Character. Group label.}
\item{\code{slope}}{Numeric. Slope estimate for
\code{distance_from_anchor} (units: change in \code{log_norm_cov} per
base pair). Typically negative when coverage decays away from
the anchor.}
\item{\code{se_slope}}{Numeric. Standard error of the slope estimate.}
\item{\code{df}}{Integer. Residual degrees of freedom of the sample's
linear fit (approximately \code{n_bins - 2}).}
\item{\code{n_bins}}{Integer. Number of bins used in the fit after
applying \code{window} and dropping non-finite rows.}
}
}
\description{
Fits a simple linear regression per sample of the form
\code{log_norm_cov ~ distance_from_anchor} and returns each sample's slope
estimate, standard error, and residual degrees of freedom. This is a
diagnostic and visualization aid, not a formal test.
}
\details{
Per-sample slope estimates are useful for:
\itemize{
\item Inspecting the spread of decay rates within each treatment group,
e.g. as a boxplot (\code{\link[=plot_decay_slopes]{plot_decay_slopes()}}).
\item Detecting outlier samples whose decay is qualitatively different
from others in the same group.
\item Sanity-checking that the decay direction (typically negative slope
in log space) matches expectations.
}

Per-sample slopes are \emph{not} recommended as the basis for a formal
group comparison. The two-stage procedure (per-sample slope, then
compare across groups) throws away the uncertainty in each per-sample
fit, weighting well-fit and poorly-fit samples equally. Use
\code{\link[=test_decay_slopes]{test_decay_slopes()}} instead, which fits a single mixed model to
all data at once and preserves within-sample precision information.

Samples with fewer than \code{min_bins} bins after distance filtering are
skipped with a warning. This can happen when \code{window} is small
relative to \code{bin_size} (few bins remain within the window) or when a
sample's coverage file yielded few usable bins after normalization.
}
\examples{
set.seed(1)
fake <- do.call(rbind, lapply(paste0("s", 1:4), function(sname) {
    tibble::tibble(
        sample = sname,
        group  = if (sname \%in\% c("s1", "s2")) "g1" else "g2",
        bin_mid = seq(500, 100000, by = 1000),
        log_norm_cov = -1e-5 * seq(500, 100000, by = 1000) +
            rnorm(100, sd = 0.1)
    )
}))
fit_decay_slopes(fake, anchor_position = 0, window = 50000)

}
\seealso{
\code{\link[=test_decay_slopes]{test_decay_slopes()}} for the recommended formal group
comparison; \code{\link[=plot_decay_slopes]{plot_decay_slopes()}} for a per-sample slope
visualization.
}
