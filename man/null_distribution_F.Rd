% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null_distribution.R
\name{null_distribution_F}
\alias{null_distribution_F}
\title{Build a null distribution of interaction F-statistics from random anchors}
\usage{
null_distribution_F(
  combined,
  genome_length,
  n_random = 500L,
  window = NULL,
  exclude = NULL,
  seed = NULL,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{combined}{A data.frame or tibble from \code{\link[=load_all_samples]{load_all_samples()}}.
Must contain columns \code{sample}, \code{group}, \code{bin_mid}, and
\code{log_norm_cov}.}

\item{genome_length}{Positive integer. Reference genome length in
base pairs. Random anchor positions are drawn uniformly from
\verb{[1, genome_length]}.}

\item{n_random}{Positive integer. Number of random anchor positions.
Default 500. The minimum achievable empirical p-value is
approximately \code{1 / (n_random + 1)}, so higher values give finer
resolution at the tail; 500 is a reasonable default for typical
experimental screens, 1000 or more for publication-grade
significance claims.}

\item{window}{Optional positive numeric scalar. Passed through
unchanged to each random-anchor call of \code{\link[=test_decay_slopes]{test_decay_slopes()}}.
Use the same value that will be used at the observed anchor of
interest, so the null and observed statistics come from
comparable fits.}

\item{exclude}{Optional length-2 numeric vector \code{c(low, high)}. If
supplied, random positions falling in \verb{[low, high]} are rejected.
Useful for excluding a band around a known anchor of interest so
the null does not include overlapping windows. Ignored if \code{NULL}.}

\item{seed}{Optional integer. Sets \code{set.seed()} for reproducibility
of the sampled positions. If \code{NULL}, no seed is set.}

\item{BPPARAM}{A \code{BiocParallel::BiocParallelParam} instance
controlling parallelism. Default \code{\link[BiocParallel:SerialParam-class]{BiocParallel::SerialParam()}}.
Use \link[BiocParallel:MulticoreParam-class]{BiocParallel::MulticoreParam} on Unix-like systems or
\link[BiocParallel:SnowParam-class]{BiocParallel::SnowParam} on Windows.}
}
\value{
A numeric vector of length at most \code{n_random} containing
the Kenward-Roger F-statistics from successful fits at the
sampled random anchor positions. Non-finite values from failed
fits are removed.
}
\description{
Repeatedly refits the linear mixed model of \code{\link[=test_decay_slopes]{test_decay_slopes()}} at
random anchor positions drawn uniformly from the genome, and returns
the vector of Kenward-Roger F-statistics for the distance-by-group
interaction at each random anchor. This vector is the null against
which the observed F at a scientifically-motivated anchor (e.g.
OriLyt) can be compared.
}
\details{
The rationale is that a decay-vs-group interaction can arise at any
genomic position for many reasons (assembly artifacts, repetitive
regions, gene expression differences that happen to correlate with
treatment). To attribute a significant interaction specifically to
the anchor of interest, we need to show that the effect at that
anchor is stronger than at random positions elsewhere in the genome.
This is exactly what the null distribution provides.

Each iteration:
\enumerate{
\item Draws a random position uniformly from \verb{[1, genome_length]}
(optionally excluding a region near the observed anchor).
\item Recomputes \code{distance_from_anchor} for every bin relative to
that random position.
\item Refits the LMM and extracts the Kenward-Roger F-statistic for the
\code{distance_from_anchor:group} interaction.
}

Failed fits (singular models, non-converging optimizers) are dropped
silently. The returned vector length may therefore be smaller than
\code{n_random}; the actual retention rate is a useful diagnostic and is
reported by \code{\link[=test_decay_significance]{test_decay_significance()}}.

Runtime scales linearly in \code{n_random} times the cost of one LMM fit.
On typical HCMV data (~30 samples, ~240 bins each), one fit takes on
the order of one to a few seconds, so \code{n_random = 500} on one core
takes roughly 10 to 30 minutes. Parallelize with \code{BPPARAM = BiocParallel::MulticoreParam(workers = N)}.
}
\examples{
\donttest{
set.seed(1)
fake <- do.call(rbind, lapply(paste0("s", 1:8), function(sname) {
    grp <- if (as.integer(sub("s", "", sname)) <= 4) "g1" else "g2"
    tibble::tibble(
        sample = sname, group = grp,
        bin_mid = seq(500, 100000, by = 1000),
        log_norm_cov = rnorm(100, sd = 0.5)
    )
}))
null_F <- null_distribution_F(
    fake, genome_length = 100000, n_random = 10, seed = 1
)
summary(null_F)
}

}
\references{
Phipson B, Smyth GK (2010). Permutation p-values should never be
zero: calculating exact p-values when permutations are randomly
drawn. \emph{Statistical Applications in Genetics and Molecular Biology}
9(1), Article 39. \doi{10.2202/1544-6115.1585}
}
\seealso{
\code{\link[=empirical_pvalue]{empirical_pvalue()}} for the p-value formula that consumes
this null; \code{\link[=test_decay_significance]{test_decay_significance()}} for the end-to-end wrapper.
}
