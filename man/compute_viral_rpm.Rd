% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpm.R
\name{compute_viral_rpm}
\alias{compute_viral_rpm}
\title{Compute viral reads-per-million (RPM)}
\usage{
compute_viral_rpm(viral_reads, human_reads)
}
\arguments{
\item{viral_reads}{Numeric vector. Number of viral reads per sample.
Must be non-negative and the same length as \code{human_reads}.}

\item{human_reads}{Numeric vector. Number of human reads per sample.
Must be non-negative and the same length as \code{viral_reads}.}
}
\value{
Numeric vector of RPM values, one per input sample. Values
are \code{NA_real_} for samples with zero human reads.
}
\description{
Vectorized calculation of viral RPM given per-sample viral read
counts and per-sample human read counts. RPM is defined as
\code{(viral_reads / human_reads) * 1e6}.
}
\details{
RPM here normalizes viral read yield against sample-specific
sequencing depth. It answers "for every million reads that mapped
to the human host, how many mapped to the virus?" This is more
informative than raw viral read counts when comparing samples with
different total sequencing depths.

In the Bosco lab pipeline, \code{viral_reads} typically comes from
breseq's \code{output/summary.json} field
\code{.reads.total_reads}, and \code{human_reads} typically comes from
the concordant read count in bowtie2's alignment log
(\code{Illumina_GRCh38.txt}). Both quantities are extracted per sample
by the accompanying shell scripts and combined into a TSV that
can be read into R with \code{read.delim()} or \code{\link[utils:read.table]{utils::read.table()}}.

If any element of \code{human_reads} is zero, the corresponding RPM is
returned as \code{NA_real_} and a warning is issued.
}
\examples{
compute_viral_rpm(
    viral_reads = c(12345, 67890, 45000),
    human_reads = c(1e7, 1.2e7, 9e6)
)

}
\seealso{
\code{\link[=plot_rpm_by_group]{plot_rpm_by_group()}} for the standard summary
visualization.
}
