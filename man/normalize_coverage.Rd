% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_io.R
\name{normalize_coverage}
\alias{normalize_coverage}
\title{Median-normalize binned coverage}
\usage{
normalize_coverage(binned, epsilon = 1e-05)
}
\arguments{
\item{binned}{A \link[data.table:data.table]{data.table::data.table} as returned by
\code{\link[=bin_coverage]{bin_coverage()}}. Must contain a \code{mean_cov} column.}

\item{epsilon}{Small positive numeric added inside \code{log()}.
Default \code{1e-5}.}
}
\value{
The input table with two columns added:
\describe{
\item{\code{norm_cov}}{Numeric. \code{mean_cov} divided by per-sample median.}
\item{\code{log_norm_cov}}{Numeric. \code{log(norm_cov + epsilon)}.}
}
}
\description{
Divides each bin's mean coverage by the sample-level median coverage
across bins, yielding a scale-invariant \code{norm_cov} value comparable
across samples with different sequencing depths. Also produces
\code{log_norm_cov = log(norm_cov + epsilon)} for downstream log-linear
decay modeling.
}
\details{
Median rather than mean normalization is used because sequencing
coverage is overdispersed and can be strongly influenced by
high-coverage regions (such as the origin during replication) that
would bias the mean. After normalization, \code{norm_cov} has median
exactly 1 within each sample.

The \code{epsilon} addition inside \code{log()} prevents \code{log(0)} for
zero-coverage bins. The default \code{1e-5} is well below any meaningful
coverage value at typical viral sequencing depths.

If the sample's median coverage is zero (essentially no reads
recovered), \code{norm_cov} and \code{log_norm_cov} are set to \code{NA_real_} and
a warning is issued.
}
\examples{
set.seed(1)
binned <- data.table::data.table(
    bin       = 1:20,
    mean_cov  = rpois(20, lambda = 30),
    bin_start = seq(1, by = 1000, length.out = 20),
    bin_end   = seq(1000, by = 1000, length.out = 20),
    bin_mid   = seq(500.5, by = 1000, length.out = 20)
)
normalize_coverage(binned)

}
\seealso{
\code{\link[=bin_coverage]{bin_coverage()}}, \code{\link[=load_sample_coverage]{load_sample_coverage()}}.
}
