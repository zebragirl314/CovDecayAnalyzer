% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_io.R
\name{load_sample_coverage}
\alias{load_sample_coverage}
\title{Load, bin, and normalize coverage for a single sample}
\usage{
load_sample_coverage(
  path,
  sample_name,
  bin_size = 1000L,
  genome_length,
  epsilon = 1e-05
)
}
\arguments{
\item{path}{Character scalar. Path to the coverage file for this
sample.}

\item{sample_name}{Character scalar. Value to write into the
returned \code{sample} column. Used to identify the sample in
downstream analyses.}

\item{bin_size}{Positive integer. Bin width in base pairs. See
\code{\link[=bin_coverage]{bin_coverage()}}. Default 1000.}

\item{genome_length}{Positive integer. Reference genome length in
base pairs.}

\item{epsilon}{Positive numeric. Log-transform offset. See
\code{\link[=normalize_coverage]{normalize_coverage()}}. Default \code{1e-5}.}
}
\value{
A tibble with one row per bin and the columns from
\code{\link[=normalize_coverage]{normalize_coverage()}} plus:
\describe{
\item{\code{sample}}{Character. The \code{sample_name} argument.}
}
}
\description{
Convenience wrapper that composes \code{\link[=read_coverage_tab]{read_coverage_tab()}},
\code{\link[=bin_coverage]{bin_coverage()}}, and \code{\link[=normalize_coverage]{normalize_coverage()}} for one coverage file
and stamps the sample name onto the result. Returns a tibble
suitable for row-binding across many samples.
}
\details{
This function is layout-agnostic: it reads the file at the path you
give it. To process many samples, use \code{\link[=load_all_samples]{load_all_samples()}} which
takes a metadata table with a \code{coverage_file} column and iterates
this function under the hood with error tolerance and optional
parallelism.
}
\examples{
cov_file <- system.file(
    "extdata", "A1", "08_mutation_identification",
    "Exported.coverage.tab",
    package = "CovDecayAnalyzer"
)
out <- load_sample_coverage(
    path          = cov_file,
    sample_name   = "A1",
    bin_size      = 100,
    genome_length = 2000
)
head(out)

}
\seealso{
\code{\link[=load_all_samples]{load_all_samples()}} for multi-sample loading.
}
