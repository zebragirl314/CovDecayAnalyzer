% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_io.R
\name{bin_coverage}
\alias{bin_coverage}
\title{Bin per-base coverage into fixed-width windows}
\usage{
bin_coverage(cov, bin_size = 1000L, genome_length)
}
\arguments{
\item{cov}{A \link[data.table:data.table]{data.table::data.table} as returned by
\code{\link[=read_coverage_tab]{read_coverage_tab()}}. Must contain columns \code{position} (positive
integers) and \code{total_cov} (non-negative numeric).}

\item{bin_size}{Positive integer. Bin width in base pairs. Default
1000.}

\item{genome_length}{Positive integer. Reference genome length in
base pairs. Used to clip the final bin end.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with one row per bin:
\describe{
\item{\code{bin}}{Integer. Bin index, 1-based.}
\item{\code{mean_cov}}{Numeric. Mean total coverage in the bin.}
\item{\code{bin_start}, \code{bin_end}}{Integer. Inclusive 1-based
coordinates.}
\item{\code{bin_mid}}{Numeric. Bin midpoint used as the bin's
representative coordinate for distance calculations.}
}
}
\description{
Aggregates per-base coverage into equally-sized non-overlapping bins
by taking the mean of \code{total_cov} within each bin.
}
\details{
Binning reduces the working table size roughly \code{bin_size}-fold and
smooths local noise while preserving large-scale trends. Bins are
constructed by integer division: base at position \code{p} is assigned to
bin \code{((p - 1) \%/\% bin_size) + 1}. The final bin is right-truncated
at \code{genome_length}.

Smaller bins retain more spatial detail but produce noisier per-bin
means. Larger bins are smoother but may average across distinct
local features. The default of 1000 bp works well for HCMV-sized
genomes (~238 kb) and gives approximately 238 bins per sample.
}
\examples{
set.seed(1)
cov <- data.table::data.table(
    position  = 1:5000,
    total_cov = rpois(5000, lambda = 30)
)
bin_coverage(cov, bin_size = 1000, genome_length = 5000)

}
\seealso{
\code{\link[=normalize_coverage]{normalize_coverage()}}, \code{\link[=load_sample_coverage]{load_sample_coverage()}}.
}
