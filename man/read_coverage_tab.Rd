% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_io.R
\name{read_coverage_tab}
\alias{read_coverage_tab}
\title{Read a per-base coverage file}
\usage{
read_coverage_tab(path)
}
\arguments{
\item{path}{Character scalar. Absolute or relative path to a
coverage file. The file must exist and be readable.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with one row per genomic position.
All columns from the input file are preserved. One additional
column is added:
\describe{
\item{\code{total_cov}}{Integer. Sum of \code{unique_top_cov},
\code{unique_bot_cov}, \code{redundant_top_cov}, and \code{redundant_bot_cov}.}
}
}
\description{
Reads a tab-delimited per-base coverage file and returns a
\link[data.table:data.table]{data.table::data.table} with total per-base coverage computed as
the sum of the four strand- and uniqueness-specific coverage columns.
}
\details{
The file must be tab-delimited with a header row and contain at
minimum the columns \code{position}, \code{unique_top_cov}, \code{unique_bot_cov},
\code{redundant_top_cov}, and \code{redundant_bot_cov}. Any additional columns
are read but ignored.

This schema matches the \code{Exported.coverage.tab} file produced by
breseq's mutation identification stage. Files produced by other
tools can also be read directly if they use the same column names,
or via a shim that renames columns before writing to disk.

Total coverage is computed as
\code{unique_top_cov + unique_bot_cov + redundant_top_cov + redundant_bot_cov}.
}
\examples{
## Read one of the four simulated samples shipped with the package.
cov_file <- system.file(
    "extdata", "A1", "08_mutation_identification",
    "Exported.coverage.tab",
    package = "CovDecayAnalyzer"
)
cov <- read_coverage_tab(cov_file)
head(cov)
summary(cov$total_cov)

}
\references{
Deatherage DE, Barrick JE (2014). Identification of mutations in
laboratory-evolved microbes from next-generation sequencing data
using breseq. \emph{Methods in Molecular Biology} 1151:165-188.
\doi{10.1007/978-1-4939-0554-6_12}
}
\seealso{
\code{\link[=bin_coverage]{bin_coverage()}} for the next step in the workflow;
\code{\link[=load_sample_coverage]{load_sample_coverage()}} for a convenience wrapper that runs read,
bin, and normalize together;
\code{\link[=breseq_coverage_paths]{breseq_coverage_paths()}} for building file paths when working with
breseq output.
}
