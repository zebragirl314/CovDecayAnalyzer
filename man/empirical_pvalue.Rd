% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null_distribution.R
\name{empirical_pvalue}
\alias{empirical_pvalue}
\title{Empirical (Monte Carlo) p-value with continuity correction}
\usage{
empirical_pvalue(observed, null_dist)
}
\arguments{
\item{observed}{Numeric scalar. Observed test statistic, e.g. the
F-statistic returned by \code{\link[=test_decay_slopes]{test_decay_slopes()}} at an anchor of
interest.}

\item{null_dist}{Numeric vector. Null distribution of the same test
statistic, e.g. as returned by \code{\link[=null_distribution_F]{null_distribution_F()}}. Non-finite
entries are dropped.}
}
\value{
A numeric scalar between \code{1 / (n + 1)} and 1, where \code{n} is
the number of finite values in \code{null_dist}.
}
\description{
Computes a right-tailed empirical p-value from an observed test
statistic and a null distribution obtained by resampling, using the
\code{(k + 1) / (n + 1)} continuity correction of Phipson & Smyth (2010).
}
\details{
The formula is

\deqn{p = (k + 1) / (n + 1)}{p = (k + 1) / (n + 1)}

where \code{k} is the number of null values greater than or equal to the
observed statistic and \code{n} is the size of the null distribution.

The \code{+1} in both numerator and denominator is not an ad-hoc smoothing
term; it is the correct exact p-value expression for Monte Carlo
permutation tests where permutations are sampled with replacement,
as derived by Phipson & Smyth (2010). Using the uncorrected estimator
\code{k / n} produces p-values that are systematically understated by
approximately \code{1 / n}, which becomes serious in multiple testing
contexts and can produce impossible p-values of exactly zero.

Non-finite values in \code{null_dist} are dropped before computation.

The minimum achievable p-value is \code{1 / (length(null_dist) + 1)}, so
Monte Carlo p-values are always bounded strictly above zero.
}
\examples{
set.seed(1)
null_F <- rf(500, df1 = 3, df2 = 50)
empirical_pvalue(observed = 5, null_dist = null_F)
empirical_pvalue(observed = 100, null_dist = null_F)  # hits the floor

}
\references{
Phipson B, Smyth GK (2010). Permutation p-values should never be
zero: calculating exact p-values when permutations are randomly
drawn. \emph{Statistical Applications in Genetics and Molecular Biology}
9(1), Article 39. \doi{10.2202/1544-6115.1585}
}
\seealso{
\code{\link[=null_distribution_F]{null_distribution_F()}}, \code{\link[=test_decay_significance]{test_decay_significance()}}.
}
