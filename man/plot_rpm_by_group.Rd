% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rpm.R
\name{plot_rpm_by_group}
\alias{plot_rpm_by_group}
\title{Barplot of viral RPM by treatment group}
\usage{
plot_rpm_by_group(
  rpm_df,
  group_col = "group",
  rpm_col = "viral_rpm",
  show_points = TRUE,
  bar_fill = "#00693E",
  error_color = "orange"
)
}
\arguments{
\item{rpm_df}{A data.frame or tibble containing per-sample RPM
values. Must contain the columns named by \code{group_col} and
\code{rpm_col}. Typically produced by joining a sample metadata table
with a vector of RPM values from \code{\link[=compute_viral_rpm]{compute_viral_rpm()}}.}

\item{group_col}{Character scalar. Name of the column containing
the treatment group label. Default \code{"group"}.}

\item{rpm_col}{Character scalar. Name of the column containing the
viral RPM values. Default \code{"viral_rpm"}.}

\item{show_points}{Logical. If \code{TRUE} (default), overlay each
sample as a jittered point on top of the group bar.}

\item{bar_fill}{Character scalar. Fill color for the bars. Default
\code{"#00693E"} (Dartmouth green), matching the prior lab convention.}

\item{error_color}{Character scalar. Color of the error bar
segments. Default \code{"orange"}, matching the prior lab convention.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a barplot of mean viral RPM per treatment group with
error bars, optionally overlaying individual sample points.
}
\details{
The default error bar is \verb{mean +/- SD}, which is a plain summary of
within-group spread. Users who want a formal comparison should
first fit a suitable model (e.g. \code{lm(log(viral_rpm) ~ group)}) and
report those results in a separate table, since the barplot is a
visual summary, not a test.

Individual sample points overlaid on the bars help readers judge
whether the group mean is dominated by a single high-RPM sample
(an important sanity check in small-n experiments). Set
\code{show_points = FALSE} for a cleaner presentation when n per group
is large.
}
\examples{
set.seed(1)
rpm_df <- data.frame(
    sample     = paste0("s", 1:12),
    group      = rep(c("WT-GCV", "WT+GCV", "FV-GCV", "FV+GCV"),
                     each = 3),
    viral_rpm  = c(rnorm(3, 3000, 500), rnorm(3, 1500, 300),
                   rnorm(3, 2500, 400), rnorm(3, 1200, 250))
)
plot_rpm_by_group(rpm_df)

}
\seealso{
\code{\link[=compute_viral_rpm]{compute_viral_rpm()}}.
}
