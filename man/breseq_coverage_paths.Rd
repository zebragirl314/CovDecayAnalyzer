% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breseq.R
\name{breseq_coverage_paths}
\alias{breseq_coverage_paths}
\title{Build coverage file paths for a breseq output tree}
\usage{
breseq_coverage_paths(
  sample_names,
  base_dir,
  subdir = "08_mutation_identification",
  filename = "Exported.coverage.tab"
)
}
\arguments{
\item{sample_names}{Character vector. Sample identifiers, matching
the directory names immediately under \code{base_dir}.}

\item{base_dir}{Character scalar. Root directory containing
per-sample subdirectories.}

\item{subdir}{Character scalar. Subdirectory within each sample
folder containing the coverage file. Default
\code{"08_mutation_identification"}.}

\item{filename}{Character scalar. Name of the coverage file within
\code{subdir}. Default \code{"Exported.coverage.tab"}.}
}
\value{
A character vector of paths, one per element of
\code{sample_names}, in the same order.
}
\description{
Convenience helper that constructs full paths to per-sample
\code{Exported.coverage.tab} files under a directory of breseq output.
The result is intended to be assigned to the \code{coverage_file} column
of a metadata table before passing to \code{\link[=load_all_samples]{load_all_samples()}}.
}
\details{
In a standard breseq run, each sample's mutation identification
outputs are written to
\verb{<base_dir>/<sample>/08_mutation_identification/}, with the
per-base coverage table at \code{Exported.coverage.tab} within that
directory. This function assembles those paths.

If your breseq output uses non-default subdirectory or filename
conventions (older or forked breseq versions, custom pipelines),
override \code{subdir} or \code{filename} accordingly.

If you do not have breseq output at all, you do not need this
function: fill the \code{coverage_file} column of your metadata table
by any other means (for example, \code{list.files()}, \code{file.path()}, or
a \code{dplyr::mutate()} call) and pass the metadata to
\code{\link[=load_all_samples]{load_all_samples()}}.

Paths are constructed as strings and returned without checking that
the files actually exist. Missing files are reported by
\code{\link[=load_all_samples]{load_all_samples()}} when it tries to read them.
}
\examples{
## Using the shipped simulated example data.
extdata_dir <- system.file("extdata", package = "CovDecayAnalyzer")
breseq_coverage_paths(
    sample_names = c("A1", "A2", "B1", "B2"),
    base_dir     = extdata_dir
)

## Typical usage: build the `coverage_file` column of a metadata
## table before calling load_all_samples().
meta <- data.frame(
    sample = c("A1", "A2", "B1", "B2"),
    group  = c("WT-GCV", "WT-GCV", "WT+GCV", "WT+GCV"),
    stringsAsFactors = FALSE
)
meta$coverage_file <- breseq_coverage_paths(meta$sample, extdata_dir)
head(meta)

}
\seealso{
\code{\link[=load_all_samples]{load_all_samples()}}, \code{\link[=read_coverage_tab]{read_coverage_tab()}}.
}
